const notas = [6.7, 7.4, 9.8, 8.1, 7.7]

for (let i in notas) {
    console.log(i, notas[i])
}

const pessoa = {
    nome: 'Ana',
    sobrenome: 'Silva',
    idade: 29,
    peso: 64
}

for(let atributos in pessoa) {
    console.log(`${atributos} = ${pessoa[atributos]}`)
}



/*const numeros = [0, 4, 6, 8, 9]
for(let i in numeros) {
    console.log(i, numeros[i])
}

const chaveValor = {
    nome: "Emerson",
    idade: 18,
    cidade: "Porto Alegre",
    profissão: "Programador"
}

for(let atributtes in chaveValor){
    console.log(`${atributtes} = ${chaveValor[atributtes]}`)
}*/





















